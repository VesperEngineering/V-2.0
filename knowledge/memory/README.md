# Approved memories

Place reviewed durable facts, preferences, decisions, and stable conventions
here. Start from `../templates/memory.md`, give every note a stable unique
`vesper_id`, and set `vesper_status: approved` only after human review.

Do not store task progress, temporary TODOs, secrets, credentials, raw market
data, or claims that should instead be supported by repository evidence.
