# Archived memories

Store archived `memory` notes here. Every admitted note must keep its stable
`vesper_id`, use `vesper_kind: memory`, `vesper_status: archived`, and
`vesper_retention: adaptive`.

These files are searchable archive context, not active approved knowledge.
